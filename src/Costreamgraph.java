public class Costreamgraph
{
    // Map<Stream, Set<Stream>>
    // Map<StreamPerson, Set<Streamperson>>

    // Speicher als Variable das Maximum
    // Map<Streamperson, Streamperson>

    // Oder extra Klasse Edges()
    // Variablen s1, s2, w


    // Für die Intersection:
    // retainAll()
    // Vorher kopieren!!!!

    // String toString()

    // toText()

    // Aufbau:
    // for-Schleife
        // maximum = -1
        // maxStream = Map<s1, null>
        // edges = Map<s1, null>
        // for-Schleife:
            // den Betrag der Schnittmenge berechenen
            // falls > 0, dann erstelle / füge hinzu in die Menge von StreamPerson s1
            // Vergleiche mit dem Maximum des PersonStreams s1

}